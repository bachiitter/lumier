{
  "name": "playground",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .lumier src lumier.config.ts .gitignore",
    "dev": "bun run lumier dev",
    "deploy": "bun run lumier deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lumier": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.3"
  }
}
